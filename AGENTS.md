# Project operating instructions

- Read `PROJECT_STATE.md` first, then `11_asset_manifest.json` before acting. The manifest governs current asset IDs and statuses; older plans and review notes may be stale.
- Use only `D:\Projects\cartoons-firm-ai-generated` as the project root. Do not rewrite historical paths unless an operational path is actually broken.
- Workflow: `NOT_GENERATED` -> `GENERATED` -> `APPROVED`. Only explicit human approval permits approval; automated checks never do.
- Never modify or regenerate an approved asset without explicit human instruction, including when an audit identifies a defect.
- Generate only the explicitly requested asset, then stop. Do not advance the queue or expand the requested scope.
- Use approved assets as canonical identity and spatial continuity references. Source panels `1.jpg` through `36.jpg` govern story-specific pose, staging, direction, props, and expression. Preserve source direction; do not accidentally mirror actions.
- Keep young/afterlife Ah Lim separate from his elderly hospital references. Both are CHAR_01.
- Anna is CHAR_08. Never recreate CHAR_09 Anna or treat obsolete CHAR_08/Kim Fook mappings as current. Kim Fook coverage is unresolved; do not assign an ID yourself.
- Hospital extras are `EXTRA_HOSPITAL_STAFF_GROUP`. Never restore the obsolete visitor-group concept; leave stale files untouched unless deletion is explicitly authorized.
- Keep action references isolated with minimal necessary support furniture. Defer motion FX to shots/video. Reuse approved props and locations; composite important text deterministically.
- Consult the recovery findings before using the missing watch, duplicated Ah Lim profile, or stale shot dependencies. Report discrepancies without silently changing statuses.
